import styled from 'styled-components';

const BtnWrapper = styled.div`
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 10px;
  margin-top: 10px;
`;

const Button = styled.button`
  text-transform: capitalize;
`;

export { Button, BtnWrapper };
